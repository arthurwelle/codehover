% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ch_hover.R
\name{ch_hover_chunk}
\alias{ch_hover_chunk}
\title{Automatic code hover from a knitr chunk}
\usage{
ch_hover_chunk(label, ...)
}
\arguments{
\item{label}{(string) The chunk label.}

\item{...}{Passed on to [ch_hover()].}
}
\value{
See [ch_hover()].
}
\description{
Reads the code of a named knitr chunk (via the public
  `knitr::knit_code` API) and passes it to [ch_hover()]. This keeps the
  source document clean: write the plot in a normal chunk with
  `eval=FALSE`, then call `ch_hover_chunk("<label>")` inline or in a
  later chunk.
}
\examples{
\dontrun{
# ```{r myplot, eval=FALSE}
# ggplot(mtcars, aes(wt, mpg)) +
#   geom_point()
# ```
# `r ch_hover_chunk("myplot")`
}

}
